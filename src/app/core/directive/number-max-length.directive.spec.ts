import { NumberMaxLengthDirective } from './number-max-length.directive';

describe('NumberMaxLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberMaxLengthDirective();
    expect(directive).toBeTruthy();
  });
});
